import array as arr
x=arr.array([7,6,5,4])
y=arr.array([4,3,2,1])
int total=0
int count=array.len(x)
for array.len(x)
    total+=x(count-1)
    total+=y(count-1)
    count--
    if count==0
        close()
print(total)
return
